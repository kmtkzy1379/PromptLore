# fly.toml app configuration file generated for promptlore on 2026-04-14T00:32:18+09:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'promptlore'
primary_region = 'nrt'
console_command = '/rails/bin/rails console'

# Linux swap on the Machine (no extra RAM charge). Absorbs short spikes; slower than RAM.
swap_size_mb = 1024

[build]

[env]
  RAILS_ENV = 'production'
  RAILS_LOG_LEVEL = 'info'
  SOLID_QUEUE_IN_PUMA = 'true'
  WEB_CONCURRENCY = '0'
  RAILS_MAX_THREADS = '2'
  PORT = '8080'
  # YJIT trades RAM for speed; off saves tens of MB on small VMs.
  RUBY_YJIT_ENABLE = '0'

[processes]
  app = './bin/rails server -b 0.0.0.0'

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = 'off'
  auto_start_machines = true
  min_machines_running = 1
  processes = ['app']

  [http_service.concurrency]
    type = 'connections'
    hard_limit = 16
    soft_limit = 12

  [[http_service.checks]]
    interval = '30s'
    timeout = '5s'
    grace_period = '60s'
    method = 'GET'
    path = '/up'

[[vm]]
  memory = '512mb'
  cpu_kind = 'shared'
  cpus = 1

[mounts]
  source = 'promptlore_storage'
  destination = '/rails/storage'
